const [a,b,c,d] = [3 ,5, 1, 15]

const soma = a + b + c + d 
const subtracao = d - b
const multiplicacao = a * b
const divisao = d / a
const modulo = a % 2

console.log(soma, subtracao, multiplicacao, divisao, modulo)
//d++ 'post fix' (depois do operando)
//++d 'pre fix' (antes do operando)
// + d 'in fix' (entre o operando)
// -a 'operador unario' (quando opera em cima de 1 dado)
//operadores binarios são aqueles que operam com 2 dados